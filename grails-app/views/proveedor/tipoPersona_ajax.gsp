<g:select id="tipoPersona" name="tipoPersona.id" from="${lista}" optionKey="${{it.id}}" required="" value="${proveedorInstance?.tipoPersona?.id}" optionValue="${{it.descripcion}}"
          class="many-to-one form-control" />